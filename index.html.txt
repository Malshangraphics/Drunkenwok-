<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DrunkenWok at Fergs | Thai Fusion Restaurant | St. Petersburg, FL</title>
    <meta name="description" content="Bold Thai fusion flavors in the heart of St. Pete. Late-night eats, signature bowls, and outdoor seating. Order online or visit us on Central Ave!">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --color-bg: #0d0d0d;
            --color-bg-light: #1a1a1a;
            --color-chili: #e63946;
            --color-chili-dark: #c62828;
            --color-lime: #2ecc71;
            --color-orange: #f39c12;
            --color-gold: #f1c40f;
            --color-purple: #9b59b6;
            --color-cyan: #00bfff;
            --color-text: #ffffff;
            --color-text-muted: #a0a0a0;
            --font-heading: 'Bebas Neue', sans-serif;
            --font-body: 'Inter', sans-serif;
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font-body);
            background-color: var(--color-bg);
            color: var(--color-text);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: var(--color-bg);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--color-chili);
            border-radius: 4px;
        }

        /* Navigation */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            padding: 1rem 5%;
            transition: var(--transition);
            background: transparent;
        }

        .navbar.scrolled {
            background: rgba(13, 13, 13, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            height: 60px;
            transition: var(--transition);
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .nav-links {
            display: flex;
            gap: 2.5rem;
            list-style: none;
            align-items: center;
        }

        .nav-links a {
            color: var(--color-text);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            transition: var(--transition);
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--color-lime);
            transition: var(--transition);
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .nav-links a:hover {
            color: var(--color-lime);
        }

        .btn-order-nav {
            background: var(--color-chili);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: var(--transition);
            box-shadow: 0 4px 15px rgba(230, 57, 70, 0.4);
        }

        .btn-order-nav:hover {
            background: var(--color-chili-dark);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(230, 57, 70, 0.6);
        }

        .mobile-menu-toggle {
            display: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--color-text);
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1553621042-f6e147245754?w=1920') center/cover;
            z-index: -2;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, rgba(13, 13, 13, 0.7) 0%, rgba(13, 13, 13, 0.9) 100%);
            z-index: -1;
        }

        .hero-content {
            text-align: center;
            padding: 2rem;
            max-width: 900px;
            animation: fadeInUp 1s ease-out;
        }

        .hero-logo {
            width: 200px;
            margin-bottom: 2rem;
            filter: drop-shadow(0 0 30px rgba(0, 191, 255, 0.3));
            animation: float 3s ease-in-out infinite;
        }

        .hero h1 {
            font-family: var(--font-heading);
            font-size: clamp(4rem, 10vw, 8rem);
            letter-spacing: 0.05em;
            margin-bottom: 0.5rem;
            text-shadow: 0 0 40px rgba(230, 57, 70, 0.5);
            background: linear-gradient(135deg, #fff 0%, var(--color-gold) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-subtitle {
            font-size: clamp(1.2rem, 3vw, 1.8rem);
            color: var(--color-lime);
            font-weight: 600;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .hero-tagline {
            font-size: clamp(1rem, 2vw, 1.3rem);
            color: var(--color-gold);
            font-style: italic;
            margin-bottom: 2.5rem;
            opacity: 0.9;
        }

        .hero-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            text-decoration: none;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.95rem;
            border: 2px solid transparent;
        }

        .btn-primary {
            background: var(--color-chili);
            color: white;
            box-shadow: 0 4px 20px rgba(230, 57, 70, 0.5);
            animation: pulse 2s infinite;
        }

        .btn-primary:hover {
            background: var(--color-chili-dark);
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(230, 57, 70, 0.7);
        }

        .btn-secondary {
            background: transparent;
            color: white;
            border-color: var(--color-lime);
        }

        .btn-secondary:hover {
            background: var(--color-lime);
            color: var(--color-bg);
            transform: translateY(-3px);
        }

        .btn-outline {
            background: transparent;
            color: white;
            border-color: rgba(255, 255, 255, 0.3);
        }

        .btn-outline:hover {
            border-color: white;
            background: rgba(255, 255, 255, 0.1);
        }

        /* Section Styles */
        section {
            padding: 6rem 5%;
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 4rem;
        }

        .section-header h2 {
            font-family: var(--font-heading);
            font-size: clamp(2.5rem, 5vw, 4rem);
            letter-spacing: 0.05em;
            margin-bottom: 1rem;
            position: relative;
            display: inline-block;
        }

        .section-header h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: var(--color-chili);
            border-radius: 2px;
        }

        .section-header p {
            color: var(--color-text-muted);
            font-size: 1.1rem;
            max-width: 600px;
            margin: 1.5rem auto 0;
        }

        /* About Section */
        .about {
            background: var(--color-bg);
        }

        .about-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .about-content h2 {
            font-family: var(--font-heading);
            font-size: clamp(2rem, 4vw, 3.5rem);
            margin-bottom: 1.5rem;
            color: var(--color-orange);
        }

        .about-content h2::after {
            content: '';
            display: block;
            width: 60px;
            height: 4px;
            background: var(--color-chili);
            margin-top: 0.5rem;
            border-radius: 2px;
        }

        .about-content p {
            color: var(--color-text-muted);
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            line-height: 1.8;
        }

        .feature-box {
            background: var(--color-bg-light);
            padding: 1.5rem;
            border-radius: 12px;
            border-left: 4px solid var(--color-lime);
            margin-top: 2rem;
        }

        .feature-box h3 {
            color: var(--color-lime);
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .about-image {
            position: relative;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        }

        .about-image img {
            width: 100%;
            height: 500px;
            object-fit: cover;
            transition: var(--transition);
        }

        .about-image:hover img {
            transform: scale(1.05);
        }

        .highlights {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .highlight-item {
            text-align: center;
            padding: 1.5rem;
            background: var(--color-bg-light);
            border-radius: 12px;
            transition: var(--transition);
        }

        .highlight-item:hover {
            transform: translateY(-5px);
            background: rgba(46, 204, 113, 0.1);
        }

        .highlight-item i {
            font-size: 2rem;
            color: var(--color-lime);
            margin-bottom: 0.5rem;
        }

        .highlight-item h4 {
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Featured Dishes */
        .featured {
            background: linear-gradient(to bottom, var(--color-bg), var(--color-bg-light));
        }

        .featured-scroll {
            display: flex;
            gap: 2rem;
            overflow-x: auto;
            padding: 1rem 0 2rem;
            scroll-snap-type: x mandatory;
            -webkit-overflow-scrolling: touch;
        }

        .featured-scroll::-webkit-scrollbar {
            height: 6px;
        }

        .featured-scroll::-webkit-scrollbar-thumb {
            background: var(--color-chili);
            border-radius: 3px;
        }

        .featured-card {
            flex: 0 0 350px;
            scroll-snap-align: start;
            background: var(--color-bg);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            transition: var(--transition);
            position: relative;
        }

        .featured-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 60px rgba(230, 57, 70, 0.3);
        }

        .featured-card img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            transition: var(--transition);
        }

        .featured-card:hover img {
            transform: scale(1.1);
        }

        .featured-tag {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: var(--color-chili);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .featured-content {
            padding: 1.5rem;
        }

        .featured-content h3 {
            font-family: var(--font-heading);
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
        }

        .featured-content p {
            color: var(--color-text-muted);
            font-size: 0.95rem;
        }

        /* Menu Section */
        .menu {
            background: var(--color-bg);
        }

        .menu-tabs {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-bottom: 3rem;
            flex-wrap: wrap;
        }

        .menu-tab {
            padding: 0.75rem 2rem;
            background: transparent;
            border: 2px solid rgba(255, 255, 255, 0.1);
            color: var(--color-text);
            border-radius: 50px;
            cursor: pointer;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: var(--transition);
        }

        .menu-tab.active,
        .menu-tab:hover {
            background: var(--color-chili);
            border-color: var(--color-chili);
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .menu-item {
            background: var(--color-bg-light);
            border-radius: 16px;
            overflow: hidden;
            transition: var(--transition);
            position: relative;
        }

        .menu-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
        }

        .menu-item-image {
            position: relative;
            height: 220px;
            overflow: hidden;
        }

        .menu-item-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }

        .menu-item:hover .menu-item-image img {
            transform: scale(1.1);
        }

        .spice-level {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: rgba(13, 13, 13, 0.8);
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.9rem;
        }

        .menu-item-content {
            padding: 1.5rem;
        }

        .menu-item-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 0.75rem;
        }

        .menu-item h3 {
            font-family: var(--font-heading);
            font-size: 1.4rem;
            letter-spacing: 0.02em;
        }

        .menu-item p {
            color: var(--color-text-muted);
            font-size: 0.9rem;
            line-height: 1.6;
        }

        /* CTA Section */
        .cta {
            background: linear-gradient(135deg, var(--color-chili-dark) 0%, var(--color-chili) 100%);
            position: relative;
            overflow: hidden;
            padding: 8rem 5%;
        }

        .cta::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 1px, transparent 1px);
            background-size: 50px 50px;
            opacity: 0.3;
            animation: moveGrid 20s linear infinite;
        }

        @keyframes moveGrid {
            0% { transform: translate(0, 0); }
            100% { transform: translate(50px, 50px); }
        }

        .cta-content {
            position: relative;
            z-index: 1;
            text-align: center;
            max-width: 800px;
            margin: 0 auto;
        }

        .cta h2 {
            font-family: var(--font-heading);
            font-size: clamp(3rem, 6vw, 5rem);
            margin-bottom: 1rem;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        .cta p {
            font-size: 1.3rem;
            margin-bottom: 2.5rem;
            opacity: 0.95;
        }

        .cta-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn-white {
            background: white;
            color: var(--color-chili);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        .btn-white:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
        }

        .btn-outline-white {
            background: transparent;
            color: white;
            border-color: white;
        }

        .btn-outline-white:hover {
            background: white;
            color: var(--color-chili);
        }

        /* Testimonials */
        .testimonials {
            background: var(--color-bg-light);
        }

        .testimonials-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .testimonial-card {
            background: var(--color-bg);
            padding: 2.5rem;
            border-radius: 20px;
            position: relative;
            transition: var(--transition);
        }

        .testimonial-card:hover {
            transform: translateY(-5px);
        }

        .testimonial-card::before {
            content: '"';
            position: absolute;
            top: 1rem;
            left: 1.5rem;
            font-size: 6rem;
            color: var(--color-chili);
            opacity: 0.2;
            font-family: serif;
            line-height: 1;
        }

        .stars {
            color: var(--color-gold);
            margin-bottom: 1rem;
            font-size: 1.1rem;
        }

        .testimonial-text {
            color: var(--color-text-muted);
            font-size: 1.05rem;
            line-height: 1.8;
            margin-bottom: 1.5rem;
            position: relative;
            z-index: 1;
        }

        .testimonial-author {
            font-weight: 600;
            color: var(--color-lime);
        }

        .testimonial-date {
            font-size: 0.85rem;
            color: var(--color-text-muted);
        }

        /* Loyalty Section */
        .loyalty {
            background: var(--color-bg);
            position: relative;
        }

        .loyalty-container {
            max-width: 1000px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .loyalty-content h2 {
            font-family: var(--font-heading);
            font-size: clamp(2rem, 4vw, 3rem);
            color: var(--color-lime);
            margin-bottom: 1rem;
        }

        .loyalty-content p {
            color: var(--color-text-muted);
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }

        .loyalty-form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .loyalty-form input {
            padding: 1rem 1.5rem;
            border-radius: 50px;
            border: 2px solid rgba(255, 255, 255, 0.1);
            background: var(--color-bg-light);
            color: white;
            font-size: 1rem;
            transition: var(--transition);
        }

        .loyalty-form input:focus {
            outline: none;
            border-color: var(--color-lime);
        }

        .loyalty-form input::placeholder {
            color: var(--color-text-muted);
        }

        .btn-lime {
            background: var(--color-lime);
            color: var(--color-bg);
            border: none;
            cursor: pointer;
            justify-content: center;
        }

        .btn-lime:hover {
            background: #27ae60;
            transform: translateY(-2px);
        }

        .benefits-list {
            margin-top: 2rem;
        }

        .benefit-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 0.75rem;
            color: var(--color-text-muted);
        }

        .benefit-item i {
            color: var(--color-lime);
        }

        .loyalty-image {
            position: relative;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
        }

        .loyalty-image img {
            width: 100%;
            height: 400px;
            object-fit: cover;
        }

        /* Location Section */
        .location {
            background: var(--color-bg-light);
        }

        .location-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: start;
        }

        .location-info h2 {
            font-family: var(--font-heading);
            font-size: clamp(2rem, 4vw, 3rem);
            margin-bottom: 2rem;
            color: var(--color-cyan);
        }

        .info-block {
            margin-bottom: 2rem;
        }

        .info-block h3 {
            color: var(--color-lime);
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 0.75rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .info-block p {
            color: var(--color-text);
            font-size: 1.1rem;
            line-height: 1.6;
        }

        .hours-grid {
            display: grid;
            gap: 0.5rem;
        }

        .hours-row {
            display: flex;
            justify-content: space-between;
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .hours-row.highlight {
            color: var(--color-lime);
            font-weight: 600;
        }

        .social-links {
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
        }

        .social-links a {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--color-bg);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--color-text);
            font-size: 1.3rem;
            transition: var(--transition);
        }

        .social-links a:hover {
            background: var(--color-chili);
            transform: translateY(-3px);
        }

        .map-container {
            background: var(--color-bg);
            border-radius: 20px;
            overflow: hidden;
            height: 100%;
            min-height: 400px;
            position: relative;
        }

        .map-placeholder {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
            flex-direction: column;
            gap: 1rem;
        }

        .map-placeholder i {
            font-size: 4rem;
            color: var(--color-chili);
        }

        .map-placeholder p {
            color: var(--color-text-muted);
        }

        /* Footer */
        footer {
            background: var(--color-bg);
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding: 4rem 5% 2rem;
        }

        .footer-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-logo {
            height: 80px;
            opacity: 0.8;
        }

        .footer-links h4 {
            color: var(--color-lime);
            margin-bottom: 1rem;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .footer-links ul {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 0.5rem;
        }

        .footer-links a {
            color: var(--color-text-muted);
            text-decoration: none;
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: var(--color-chili);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: var(--color-text-muted);
            font-size: 0.9rem;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        @keyframes pulse {
            0%, 100% { box-shadow: 0 4px 20px rgba(230, 57, 70, 0.5); }
            50% { box-shadow: 0 4px 40px rgba(230, 57, 70, 0.8); }
        }

        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease-out;
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Mobile Responsive */
        @media (max-width: 968px) {
            .nav-links {
                position: fixed;
                top: 0;
                right: -100%;
                width: 80%;
                max-width: 400px;
                height: 100vh;
                background: var(--color-bg);
                flex-direction: column;
                padding: 6rem 2rem 2rem;
                transition: var(--transition);
                gap: 2rem;
            }

            .nav-links.active {
                right: 0;
            }

            .mobile-menu-toggle {
                display: block;
                z-index: 1001;
            }

            .about-container,
            .loyalty-container,
            .location-container {
                grid-template-columns: 1fr;
            }

            .about-image {
                order: -1;
            }

            .loyalty-image {
                display: none;
            }

            .footer-container {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .highlights {
                grid-template-columns: 1fr;
            }

            .hero-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
        }

        @media (max-width: 480px) {
            .featured-card {
                flex: 0 0 280px;
            }

            .menu-grid {
                grid-template-columns: 1fr;
            }

            .testimonials-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>

    <!-- Navigation -->
    <nav class="navbar" id="navbar">
        <div class="nav-container">
            <img src="https://via.placeholder.com/200x80/0d0d0d/00bfff?text=DRUNKENWOK" alt="DrunkenWok Logo" class="logo" id="navLogo">
            <ul class="nav-links" id="navLinks">
                <li><a href="#home">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#menu">Menu</a></li>
                <li><a href="#location">Contact</a></li>
                <li><a href="tel:7274208923" class="btn-order-nav">Order Now</a></li>
            </ul>
            <div class="mobile-menu-toggle" id="mobileMenuToggle">
                <i class="fas fa-bars"></i>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <img src="https://via.placeholder.com/200x200/0d0d0d/00bfff?text=DW" alt="DrunkenWok Logo" class="hero-logo">
            <h1>DRUNKENWOK</h1>
            <p class="hero-subtitle">Thai Fusion with Fire & Flavor</p>
            <p class="hero-tagline">"Grab a bite, catch the game, spice up your night!"</p>
            <div class="hero-buttons">
                <a href="#menu" class="btn btn-primary">
                    <i class="fas fa-utensils"></i> View Menu
                </a>
                <a href="https://order.drunkenwok.com" target="_blank" class="btn btn-secondary">
                    <i class="fas fa-shopping-bag"></i> Order Online
                </a>
                <a href="tel:7274208923" class="btn btn-outline">
                    <i class="fas fa-phone"></i> Book a Table
                </a>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section class="about" id="about">
        <div class="about-container">
            <div class="about-content reveal">
                <h2>Always an Amazing Experience</h2>
                <p>From the first bite to the last, we bring bold flavors, good vibes, and unforgettable moments. Every visit is packed with flavor and always an amazing experience!</p>
                <p>Located in the heart of St. Petersburg on Central Avenue, DrunkenWok at Fergs combines authentic Thai street food roots with creative fusion twists. Our late-night hours and vibrant atmosphere make us the perfect spot for food lovers, night owls, and anyone craving something extraordinary.</p>
                
                <div class="feature-box">
                    <h3><i class="fas fa-lightbulb"></i> Beautiful Outdoor Seating</h3>
                    <p>Kick back under the lights and enjoy your meal at one of our cozy outdoor tabletops—perfect for soaking up good vibes with great food.</p>
                </div>

                <div class="highlights">
                    <div class="highlight-item">
                        <i class="fas fa-moon"></i>
                        <h4>Late Night</h4>
                        <p>Open until 2AM</p>
                    </div>
                    <div class="highlight-item">
                        <i class="fas fa-fire"></i>
                        <h4>Thai Fusion</h4>
                        <p>Bold flavors</p>
                    </div>
                    <div class="highlight-item">
                        <i class="fas fa-map-marker-alt"></i>
                        <h4>Central Ave</h4>
                        <p>Heart of St. Pete</p>
                    </div>
                </div>
            </div>
            <div class="about-image reveal">
                <img src="https://images.unsplash.com/photo-1553621042-f6e147245754?w=800" alt="DrunkenWok Signature Dish">
            </div>
        </div>
    </section>

    <!-- Featured Dishes -->
    <section class="featured">
        <div class="section-header reveal">
            <h2>Signature Creations</h2>
            <p>Our most loved dishes, crafted with passion and fire</p>
        </div>
        <div class="featured-scroll">
            <div class="featured-card">
                <span class="featured-tag">Best Seller</span>
                <img src="https://images.unsplash.com/photo-1568901346375-23c9450c58cd?w=600" alt="Wagyu Sliders">
                <div class="featured-content">
                    <h3>Wagyu Sliders</h3>
                    <p>Premium beef, elevated. Two sliders with caramelized onions, cheddar, and our signature Yum Yum Comeback Sauce.</p>
                </div>
            </div>
            <div class="featured-card">
                <span class="featured-tag" style="background: var(--color-lime); color: var(--color-bg);">Signature</span>
                <img src="https://images.unsplash.com/photo-1544025162-d76694265947?w=600" alt="Ribeye Crying Tiger Bowl">
                <div class="featured-content">
                    <h3>Ribeye Crying Tiger Bowl</h3>
                    <p>Our signature steak bowl. Marinated ribeye with Thai chimichurri, rice, and steamed broccoli.</p>
                </div>
            </div>
            <div class="featured-card">
                <span class="featured-tag" style="background: var(--color-orange);">Must Try</span>
                <img src="https://images.unsplash.com/photo-1573080496219-bb080dd4f877?w=600" alt="Drunken Fries">
                <div class="featured-content">
                    <h3>Drunken Fries</h3>
                    <p>Loaded with chopped ribeye, cheese sauce, pico de gallo, and pickled onions. Pure indulgence.</p>
                </div>
            </div>
            <div class="featured-card">
                <span class="featured-tag" style="background: var(--color-purple);">Popular</span>
                <img src="https://images.unsplash.com/photo-1555126634-323283e090fa?w=600" alt="Garlic Noodles">
                <div class="featured-content">
                    <h3>Garlic Noodles</h3>
                    <p>Savory garlic buttered noodles with succulent sautéed shrimp. A customer favorite.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Menu Section -->
    <section class="menu" id="menu">
        <div class="section-header reveal">
            <h2>Explore the Menu</h2>
            <p>Bold flavors crafted for every craving</p>
        </div>
        
        <div class="menu-tabs reveal">
            <button class="menu-tab active" data-category="starters">Starters</button>
            <button class="menu-tab" data-category="bowls">Bowls & Entrees</button>
            <button class="menu-tab" data-category="specials">Specials</button>
            <button class="menu-tab" data-category="drinks">Drinks</button>
        </div>

        <div class="menu-grid" id="menuGrid">
            <!-- Starters -->
            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1606491956689-2ea866880c84?w=600" alt="Brussel Sprouts">
                    <span class="spice-level">🌶️</span>
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Brussel Sprouts</h3>
                    </div>
                    <p>With Sweet Chili Sauce. Brussel Sprouts with Sweet chili glaze topped with pickled onions.</p>
                </div>
            </div>

            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1617093727343-374698b1b08d?w=600" alt="Crab Rangoons">
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Crab Rangoons</h3>
                    </div>
                    <p>Delicate crab meat and cream cheese in crispy wonton wrappers. Served with sweet chili sauce.</p>
                </div>
            </div>

            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1569058242253-92a9c755a0ec?w=600" alt="Shrimp Tempura">
                    <span class="spice-level">🌶️</span>
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Shrimp Tempura</h3>
                    </div>
                    <p>3 pieces of crispy shrimp tempura served with our signature Bang Bang Sauce.</p>
                </div>
            </div>

            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1568901346375-23c9450c58cd?w=600" alt="Wagyu Sliders">
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Wagyu Sliders</h3>
                    </div>
                    <p>Two Wagyu beef sliders with caramelized onions, cheddar, tomato, lettuce, and Yum Yum Comeback Sauce.</p>
                </div>
            </div>

            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1573080496219-bb080dd4f877?w=600" alt="Drunken Fries">
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Drunken Fries</h3>
                    </div>
                    <p>Crispy fries topped with chopped Ribeye, cheese sauce, pico de gallo, and pickled onions.</p>
                </div>
            </div>

            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1563245372-f21724e3856d?w=600" alt="Korean Fried Chicken Bao">
                    <span class="spice-level">🌶️</span>
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Korean Fried Chicken Bao</h3>
                    </div>
                    <p>Crispy chicken in soft bao bun with soy-garlic sauce, cucumber, and pickled daikon.</p>
                </div>
            </div>

            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1559314809-0d155014e29e?w=600" alt="Braised BBQ Pork Belly Bao">
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Braised BBQ Pork Belly Bao</h3>
                    </div>
                    <p>Tender pork belly with soy-honey glaze in fluffy bao bun, topped with cucumbers and red peppers.</p>
                </div>
            </div>

            <div class="menu-item" data-category="starters">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1559742811-822873691df8?w=600" alt="Dynamite Shrimp">
                    <span class="spice-level">🌶️</span>
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Dynamite Shrimp</h3>
                    </div>
                    <p>Crispy golden-battered shrimp tossed in creamy bang bang sauce, served in a fluffy bao bun.</p>
                </div>
            </div>

            <!-- Bowls (initially hidden) -->
            <div class="menu-item hidden" data-category="bowls" style="display: none;">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1544025162-d76694265947?w=600" alt="Ribeye Crying Tiger Bowl">
                    <span class="spice-level">🌶️🌶️</span>
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Ribeye Crying Tiger Bowl</h3>
                    </div>
                    <p>With Thai chimichurri sauce, marinated ribeye grilled over rice with steamed broccoli.</p>
                </div>
            </div>

            <div class="menu-item hidden" data-category="bowls" style="display: none;">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1555126634-323283e090fa?w=600" alt="Garlic Noodles Bowl">
                </div>
                <div class="menu-item-content">
                    <div class="menu-item-header">
                        <h3>Garlic Noodles Bowl</h3>
                    </div>
                    <p>Savory garlic buttered noodles with fresh scallions and succulent sautéed large shrimp.</p>
                </div>
            </div>

            <div class="menu-item hidden" data-category="bowls" style="display: none;">
                <div class="menu-item-image">
                    <img src="https://images.unsplash.com/photo-1559742811-822